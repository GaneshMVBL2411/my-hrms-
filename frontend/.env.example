# Dev server proxies /api to the backend (see vite.config.ts); no env vars required for local dev.
# Set this for production builds where the frontend and backend are on different domains (e.g. Vercel + Render):
VITE_API_BASE_URL=
